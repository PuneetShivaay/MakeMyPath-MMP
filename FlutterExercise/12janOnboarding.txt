import 'package:flutter/material.dart';

void main() {
  runApp(MaterialApp(
          home: Scaffold(
              body: Center(
    child: SizedBox(
      child: Center(
        child: Stack(
          alignment: Alignment.center,
          children: <Widget>[
            Container(
              decoration: BoxDecoration(
                image: DecorationImage(
                    //BackgroundImage
                    image: NetworkImage(
                        'https://erdr.in/wp-content/uploads/2021/01/9-january-1.png'),
                    fit: BoxFit.cover),
              ),
            ),
            Positioned(
              top: 10,
              right: 5,
              child: new FlatButton(
                onPressed: () {
                  print('Skip Pressed');
                },
                shape: new RoundedRectangleBorder(
                    borderRadius: new BorderRadius.circular(30.0)),
                child: new Text('Skip',
                    style: new TextStyle(
                        color: Colors.black,
                        fontSize: 10.0,
                        fontWeight: FontWeight.bold)),
              ),
            ),
            Positioned(
                width: 150,
                top: 200,
                child: Container(                  
                  child: Image(
                      //TextImage
                      image: NetworkImage(
                          'https://erdr.in/wp-content/uploads/2021/01/path1.png')),
                )),
            Positioned(
                top: 400,
                child: Container(                  
                  child: Text(
                    
                    "Surrender to your path, forget about time or speed, and most importantly walk in joy",
                    textAlign: TextAlign.center,
                    style: TextStyle(
                      
                      color: Colors.black,
                      fontSize: 15,
                    ),
                  ),
                )),
            Positioned(
                width: 200,
                bottom: 50,
                child: Container(                 
                  child: Image(image: NetworkImage(//MainImage
                      'https://erdr.in/wp-content/uploads/2021/01/path2.png')),
                )),
          ], //<Widget>[]
        ), //Stack
      ), //Center
    ), //SizedBox
  ) //Center
              ) //Scaffold
          ) //MaterialApp
      );
}
