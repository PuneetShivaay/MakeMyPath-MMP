import 'package:flutter/material.dart';

void main() => runApp(CreatePractice());

class CreatePractice extends StatelessWidget {
  Widget build(BuildContext context) {
    return MaterialApp(
        home: Scaffold(
      body: Center(
        child: Column(
          children: <Widget>[
            Container(
              child: Image.network(
                'https://picsum.photos/250?image=9',
              ),
            ),
            Container(child: Text("Instructions")),
            Container(
                child: Text(
                    "1) Tital Duration of exam is 120 minutes. \n2) Your Clock will be set at the server. \n3) The question plate display on the right side of the screen.")),
            Container(
              child: Row(
                children: [
                  Container(
                    height: 50.0,
                    width: 50,
                    decoration: BoxDecoration(
                        color: Colors.green,
                        borderRadius: BorderRadius.all(Radius.circular(8.0))),
                  ),
                  Container(child: Text("You have answered the question")),
                ],
              ),
            ),
            Container(
              child: Row(children: [
                Container(
                  height: 50.0,
                  width: 50,
                  decoration: BoxDecoration(
                      color: Colors.red,
                      borderRadius: BorderRadius.all(Radius.circular(8.0))),
                ),
                Container(
                  child: Text("Ypu have visited but not ansered"),
                ),
              ]),
            ),
            Container(
              child: Row(
                children: [
                  Container(
                    height: 50.0,
                    width: 50,
                    decoration: BoxDecoration(
                        color: Colors.yellow,
                        borderRadius: BorderRadius.all(Radius.circular(8.0))),
                  ),
                  Container(child: Text("You have answered the question")),
                ],
              ),
            ),
            Container(
              child: Row(
                children: [
                  Container(
                    height: 50.0,
                    width: 50,
                    decoration: BoxDecoration(
                        color: Colors.yellow[700],
                        borderRadius: BorderRadius.all(Radius.circular(8.0))),
                  ),
                  Container(child: Text("You have answered the question")),
                ],
              ),
            ),
            Container(child: Row(children: [])),
          ],
        ),
      ),
    ));
  }
}
